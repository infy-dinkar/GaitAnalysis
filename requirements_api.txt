# MotionLens API — extra deps on top of requirements.txt.
# Install both:
#     pip install -r requirements.txt
#     pip install -r requirements_api.txt
fastapi>=0.115
uvicorn[standard]>=0.30
python-multipart>=0.0.9
pydantic>=2.6
pydantic[email]>=2.6

# ─── Auth + database (Phase 1) ─────────────────────────────────────
# motor    : async MongoDB driver (built on pymongo)
# pymongo  : underlying sync driver
# passlib  : password hashing — bcrypt round 12
# bcrypt   : pinned to 4.x because passlib 1.7.4 reads bcrypt.__about__
#            which was removed in bcrypt 5.0 — without this pin, signup
#            crashes with "module 'bcrypt' has no attribute '__about__'"
#            and a misleading "password cannot be longer than 72 bytes"
#            error from passlib's fallback verification path.
# python-jose : JWT encode/decode for auth tokens
# python-dotenv : loads .env file in local dev (HF Spaces uses platform env)
motor>=3.5
pymongo>=4.6
passlib>=1.7.4
bcrypt>=4.0,<5.0
python-jose[cryptography]>=3.3
python-dotenv>=1.0

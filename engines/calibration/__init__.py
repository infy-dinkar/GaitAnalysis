"""engines.calibration — shared scale calibration helpers.

Currently exposes a height-based calibration provider used by C6
Functional Reach. Future distance-based tests (D3 Single-Leg Hop,
D4 CMJ) will reuse the same module.
"""
